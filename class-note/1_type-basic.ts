//var str = "hello";

// ts 문자열 선언
let str: string = "hello";
let num: number = 10;
let arr: Array<number> = [1, 2, 3];
let heroes: Array<string> = ["Capt", "Thor", "Hulk", 10];

let items: number[] = [1, 2, 3];

// TS 튜플
let adr: [string, number] = ["gnangnam", 100];

// TS 객체
let obj: object = {};

let person: { name: string; age: number } = {
  name: "thor",
  age: 1000,
};

// 진위 값
let show: boolean = true;
